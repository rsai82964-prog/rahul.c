#include<stdio.h>
void main()
{
    int sum=0,n,a,cube;
    scanf("%d",&n);
    a=num;
    while (num!=0){
        int digit = n%10;
        cube=digit*digit*digit;
        sum=sum+cube;
        n=n/10;
    }
    if(a==sum){
        printf("the number is armstrong");
    }
    else{
        printf("the number is not armstrong");
    }
}
