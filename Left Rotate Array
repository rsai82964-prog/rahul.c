#include<stdio.h>
int main()
{
    int arr[]={1,2,3,4,5};
    int n=5,k=2,i,temp;

    while(k--)
    {
        temp=arr[0];
        for(i=0;i<n-1;i++)
            arr[i]=arr[i+1];

        arr[n-1]=temp;

    }

    for(i=0;i<n;i++)
        printf("%d ",arr[i]);
}
