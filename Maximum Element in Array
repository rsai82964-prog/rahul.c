#include<stdio.h>
int main()
{
    int arr[]={5,3,9,1,6};
    int n=5,i,max=arr[0];

    for(i=1;i<n;i++)
    {
        if(arr[i]>max) 
            max=arr[i];
    }

    printf("Maximum element = %d",max);
}
