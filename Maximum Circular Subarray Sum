#include<stdio.h>
int main()
{
    int arr[]={5,-2,3,4};
    int n=4,i,sum=0,max=arr[0];

    for(i=0;i<n;i++)
    {
        sum+=arr[i];
        if(sum>max) 
            max=sum;
        if(sum<0)
            sum=0;
    }

    printf("Maximum sum = %d",max);
}
